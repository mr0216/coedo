<div class="card">
  <%# <%= link_to image_tag(post.image, class: :card__img), post_path(post_path.id) %> %>
  <div class="card__body">
    <%# <%= link_to "#{post.title}", post_path(post.id), class: :card__title%> %>
    <p class="card__summary">
      <%# <%= "#{post.text}" %> %>
    </p>
    <%# <%= link_to "by #{post.user.nickname}", user_path(ppost.user.id), class: :card__user %> %>
  </div>
</div>